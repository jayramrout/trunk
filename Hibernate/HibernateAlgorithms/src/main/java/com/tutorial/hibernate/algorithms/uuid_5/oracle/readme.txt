
create table bank1
(
acno varchar2(32) primary key,
name varchar2(40)
);

This algorithm uses network ip address as a base value to generate string data type hexadecimal notation value for pojo class object. This value contains 32 alfa numeric characters in hexadecimal notation.