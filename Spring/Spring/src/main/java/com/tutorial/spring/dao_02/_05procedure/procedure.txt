CREATE OR REPLACE PROCEDURE GET_EMP_DATA
	(NAME IN VARCHAR,
         NO OUT NUMBER,   
	 DESG OUT VARCHAR,
         SALARY OUT NUMBER)
AS

BEGIN
       SELECT EMPLOYEE_ID, JOB_ID, SALARY
             INTO NO, DESG, SALARY 
             FROM EMPLOYEES
             WHERE FIRST_NAME = NAME;
END;