Making spring container to create spring bean class object through non static methods [instance methods].

Note: While configuring spring bean class if class attribute is not there and factory-bean, factory-method attributes are there then spring container uses instance factory method of specified factory bean class to create spring bean class object.